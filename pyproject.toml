[project]
name = "codex-harness"
version = "0.1.0"
description = "Codex-native planner/implementer/verifier harness skill"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["scripts"]
testpaths = ["tests"]

